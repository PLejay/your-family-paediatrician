---

---

<ul class="grid">
  <slot />
</ul>

<style>
  .grid {
    display: grid;
    grid-auto-rows: 1fr;
    gap: 1rem;
    list-style: none;
    padding: 0;
		width: 100%;
  }

  @media (min-width: 50em) {
    .grid {
      grid-template-columns: 1fr 1fr 1fr;
      gap: 4rem;
    }
  }
</style>
