---
import { render, type CollectionEntry } from "astro:content";
import Stars from "./ui/Stars.astro";

type Props = {
  testimonial: CollectionEntry<"testimonials">;
  compact?: boolean;
};

const { compact = false } = Astro.props;
const { Content } = await render(Astro.props.testimonial);
---

<figure class:list={["testimonial", { compact }]}>
  <Stars />
  <blockquote>
    <Content />
  </blockquote>
</figure>

<style>
  .testimonial {
    margin: 0;
    padding: 1.75rem;
    background-color: var(--card-tint);
    border: 1px solid var(--border-soft);
    border-radius: var(--radius);
  }

  blockquote {
    margin: 0.75rem 0 0;
    font-style: italic;
    font-size: 1.1875rem;
    line-height: 1.5;
    color: #2f3f3b;
  }

  blockquote :global(p) {
    margin: 0;
  }

  .testimonial.compact {
    padding: 1.375rem 1.5rem;
  }

  .compact blockquote {
    font-size: 1.0625rem;
  }
</style>
